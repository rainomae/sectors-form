# Test profile: in-memory H2 in PostgreSQL compatibility mode, so the PostgreSQL migrations run unchanged.
spring:
  datasource:
    url: jdbc:h2:mem:sectors_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
